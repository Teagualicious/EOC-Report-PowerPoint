# Developer Area

- `tests/` - pytest suite; run from project root with `python -m pytest developer/tests -q`.
- `build/` - PyInstaller one-folder build specification.
- `tools/` - developer utilities, including portable dependency installation.

Read `documentation/MODEL_HANDOFF.md` before making changes.
